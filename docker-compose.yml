version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:/app/data/reverse_academy.db
    volumes:
      - ./data:/app/data
      - ./static/binaries:/app/static/binaries:ro
    restart: unless-stopped
